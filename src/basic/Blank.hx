package basic;

/**
 * If the input is effectively empty, the output should be too.
 * @test
 * @onlyScripts []
 * @forbiddenCode ["var ", "enum "]
 * @author YellowAfterlife
 */
@:keep class Blank {
	public static inline function main() {
		//
	}
}